# 任务就绪报告：`<任务 ID / 一句话简述>`

> 本文件只针对**单个任务**，依赖同目录的「项目与环境就绪」基线文档。存在多个任务时，每个任务各建一份本模板文件，不要合并进同一个文件，也不要把项目/环境内容再抄一遍——用回链引用基线文档即可。
>
> **结构规则（必须遵守，保证多次运行一致）**：严格按本模板的章节标题与顺序填写，不新增、不删除、不重排、不改名章节；用不到的表格行写「不适用」并给理由。所有影响开发决策的结论都带证据标签（`[已验证]` / `[代码证据]` / `[用户提供]` / `[未确认]` / `[证据冲突]`）并记录来源。

## 1. 任务基线

- 任务：`<ID / 标题 / 来源链接或需求文档>`
- 生成时间（`date -u` 取值）：
- 基准分支：`<本任务从此分支切；须取证确认，不假设为 main/master>`
- 依赖的基线文档：`<项目与环境就绪报告的路径>`
- 环境在基准分支上的验证状态：就绪 / 需重验（说明原因，如基线文档在别的分支验证）
- Commit SHA / 工作树状态：

## 2. 任务就绪结论

- 任务状态：就绪 / 未就绪
- 判断理由（一句话 + 关键证据）：
- 如果现在开始开发，最大风险：

### 立即行动清单

- [ ] `<立即行动>`
- [ ] `<立即行动>`

### 未解决阻断

| 阻断 | 影响 | 需要谁提供什么 | 解除标准 |
| --- | --- | --- | --- |
|  |  |  |  |

## 3. 目标与边界

- 目标行为：
- 必须保持兼容的行为：
- 明确不在范围内：
- 验收条件：

## 4. 修改面

### 代码入口与修改范围

| 路径或模块 | 作用 | 与任务的关系 | 建议（修改 / 只读参考 / 不应触碰） | 证据 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 必要业务链路

1. `<业务链路步骤>`
2. `<业务链路步骤>`

### 类似实现

| 路径 | 可参考内容 | 不可照搬之处 | 证据 |
| --- | --- | --- | --- |
|  |  |  |  |

## 5. 开发与调试指南

> 说明「开发时如何观察和验证变化」，不代表本 Agent 已实现或完成开发后调试。

- 推荐开发方式与前置条件：
- 启动与触发入口：
- 观察点（日志 / 网络 / 状态 / 事件 / 浏览器控制台）：
- 成功判断与必须避免的回归：

### 验证路径

| 层级 | 命令或步骤 | 预期结果 | 覆盖风险 |
| --- | --- | --- | --- |
| 最小验证 |  |  |  |
| 自动化测试 |  |  |  |
| 手工验证 |  |  |  |
| 回归检查 |  |  |  |

## 6. 风险与需确认（任务相关）

| 区域 | 风险或历史坑 | 影响 | 证据 | 开发时如何处理 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 需人工确认

| 询问角色 | 问题 | 是否阻断 |
| --- | --- | --- |
| 业务 Owner / 模块 Owner / 后端接口 Owner |  | 是 / 否 |

## 7. 新会话接手说明

给新的 Coding Agent：

1. 先读基线文档，再读本文件的「任务就绪结论」「目标与边界」「开发与调试指南」。
2. 保留所有 `[未确认]` / `[证据冲突]`，不得自行升级为事实。
3. 实现范围：
4. 禁止或暂不修改：
5. 实现后必须执行（含在基准分支启动应用并确认无阻断性报错）：
6. 开始编码前仍需完成：
