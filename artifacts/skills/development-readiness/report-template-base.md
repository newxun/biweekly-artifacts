# 项目与环境就绪报告（基线文档）

> 本文件是「项目 + 环境」就绪基线，跨任务复用；每个具体任务另建一份「任务就绪」文件（见 `report-template-task.md`）并回链本文件。
>
> **结构规则（必须遵守，保证多次运行一致）**：严格按本模板的章节标题与顺序填写，不新增、不删除、不重排、不改名章节；用不到的表格行写「不适用」并给理由，不留空、不为填满而猜测。所有影响开发决策的结论都带证据标签（`[已验证]` / `[代码证据]` / `[用户提供]` / `[未确认]` / `[证据冲突]`）并记录来源与适用范围。

## 1. 基线与交接信息

- 生成时间（`date -u` 取值）：
- 仓库与远程：
- 当前分支：
- 基准分支：`<后续任务默认从此分支切；不假设为 main/master，须取证>`
- Commit SHA（`git rev-parse HEAD` 取值）：
- 工作树状态（`git status --short` 取值，含用户已有未提交改动）：
- 环境标识（操作系统、关键运行时、包管理器、容器或远程环境）：
- 生成工具或 Agent：
- 报告文件路径：
- 变化后必须重验：`<列出哪些文件/配置变化会让哪些结论失效，供新会话用 git diff 定位后降级重验>`

## 2. 环境就绪结论

- 环境状态：就绪 / 有限就绪 / 未就绪
- 判断理由（一句话 + 关键证据）：
- 有限就绪补充：允许开始的范围 / 禁止开始的范围 / 残余风险 / 用户是否明确接受（是 / 否 / 不适用）
- 如果现在开始开发，最大风险：

### 立即行动清单

- [ ] `<立即行动>`
- [ ] `<立即行动>`

### 未解决阻断

| 阻断 | 影响 | 需要谁提供什么 | 解除标准 |
| --- | --- | --- | --- |
|  |  |  |  |

## 3. 项目速览

> 只记「完成开发所需」的信息，不写项目百科。

- 项目用途与边界：
- 技术栈与运行时（语言、框架、关键依赖、依赖管理与构建工具）：
- 主要入口与关键目录职责：
- 开发 / 分支 / 提交 / Review / 发布约定：
- 关键业务概念（仅与后续开发相关的）：

## 4. 环境验证矩阵

| 检查项 | 必要 / 不适用 | 命令 | 结果（成功 / 失败 / 未执行） | 证据与适用范围 |
| --- | --- | --- | --- | --- |
| 安装依赖 |  |  |  |  |
| 生成产物 / setup |  |  |  |  |
| 启动（dev server 等） |  |  |  |  |
| 应用健康检查（实际打开运行中的应用，确认可访问且无阻断性运行时报错） |  |  |  |  |
| 构建 |  |  |  |  |
| 测试 / 最小验证 |  |  |  |  |

### 运行顺序与健康信号

1. `<启动步骤>`
2. `<启动步骤>`

- 启动成功的判断方式（不仅是进程 listening / HTTP 200，还须含页面能正常访问、关键界面渲染、无阻断性运行时报错）：
- 需要保留的进程或会话：
- 停止与清理方式：
- 环境验证所在分支：`<在哪个分支上验证的；若与「基准分支」不同，说明结论需在基准分支重验>`

### 环境问题调查记录

| 原始命令与退出码 | 脱敏错误或日志位置 | 分类（本机环境 / 项目配置 / 外部依赖 / 业务认知 / 开发方式） | 已尝试方法 | 结果与排除依据 | 下一步 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

## 5. 风险、冲突与需人工确认

### 历史负债与已知风险（仅与开发或环境相关）

| 区域 | 风险或历史负债 | 影响 | 证据 | 开发时如何处理 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 证据冲突

| 结论 | 冲突来源 | 已做验证 | 当前判断 | 对开发的影响 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

### 需人工确认

| 询问角色 | 问题 | 为什么现在问 | 是否阻断 |
| --- | --- | --- | --- |
| 项目负责人 / 模块 Owner / 业务 Owner / 发布负责人 |  |  | 是 / 否 |

## 6. 证据与命令留痕

### 关键文件与证据

| 证据 | 支持的结论 | 备注 |
| --- | --- | --- |
| `path/to/file` |  |  |

### 实际执行命令

| 命令或实验 | 目的 | 结果 | 时间或环境 |
| --- | --- | --- | --- |
|  |  |  |  |
